#ここはメモを書いていきます。


#sailコマンドのalias
#alias sail='./vendor/bin/sail' (くっ付ける)

#モデルを作成する
#sail php artisan make:model モデル名(単数形)

#コントローラーを作る
#sail php artisan make:controller コントローラー名

#Seederの実行
#sail php artisan db:seed -class=クラス名

#(DB)MySQLの設定
#DB_CONNECTION=mysql
#DB_HOST=mysql
#DB_PORT=3306
#DB_DATABASE=books
#DB_USERNAME=sail
#DB_PASSWORD=password


#Viteサーバーの立ち上げ
#sail npm run dev  (開発環境)
#sail npm run build または sail npm run watch (本番環境)

#----CodeSniffer(PHPのコード規約エラー検出・修正)----
#エラー検出
#(sail php )./vendor/bin/phpcs パスの指定

#コード修正
#(sail php) ./vendor/bin/phpcbf

#alias(エーリアス)
#alias phpcs='./vendor/bin/phpcs' 検出
#alias phpcbf='./vendor/bin/phpcbf' 修正
